# SparkDBA 演示视频 · 口播脚本

- 作品：SparkDBA（第三届 NVIDIA DGX Spark 黑客松 · 团队 GPTOPS）
- 配音：macOS `say`，声音 Tingting（zh_CN）；字幕烧录 + mov_text 软字幕 + 同名 .srt
- 时间轴由 `media/tools/build.mjs` 按实测语音时长回写，勿手改时间
- 全部画面为站点真实录屏；数字来自评测结果文件，不做修饰

## 00:00–00:09 · 片头
- **屏幕**：标题卡「SparkDBA · 让数据自己诊断，就在 DGX Spark 上」
- **口播**：「SparkDBA，让数据自己诊断，就在一台 DGX Spark 上。」

## 00:09–00:28 · 总览
- **屏幕**：首页标题、Agent 最近运行、统计卡、流水线
- **口播**：「模型、数据库和评测全部跑在这台 Spark 上。英伟达 Nemotron 三点五模型，NVFP4 量化，本地推理，数据不出机房。九个 Agent Skills 负责取证、根因和分级处置。」

## 00:28–00:53 · 故障实验室
- **屏幕**：注入「长事务未提交」→ 两个都跑 → 有技能给出持锁 pid，基线误判
- **口播**：「我们注入一个长事务未提交的故障。持锁的会话处于空闲状态，只看活动会话是看不见它的。同一个模型、同样的工具，唯一差别是有没有技能。有技能的一侧找到了持锁的进程和表，基线把它误判成了锁竞争。」

## 00:53–01:18 · 负载报告
- **屏幕**：Top-5 判定卡片、按会话切分的平均活跃会话图
- **口播**：「故障结束以后怎么办？我们把金仓的 KWR、KSH、KDDM 搬到了原生 PostgreSQL：每秒会话历史、计数器快照和 Top-5 阈值判定。即使现场已经恢复，也能按会话切分，复盘到当时的持锁者。」

## 01:18–01:48 · 工业时序
- **屏幕**：TDengine 产线良品率与设备曲线 → 注入 L2 烤箱过热 → 诊断给出设备与温升
- **口播**：「同一个智能体换一类数据。模拟工厂每秒向 TDengine 写入十五台设备的遥测。注入 L2 烤箱过热，良品率随之下降。根因顾问给出设备、温度变化和良品率变化，每个数字都能追溯到一条查询。」

## 01:48–02:02 · 智能体设计
- **屏幕**：智能体四大组件页，逐个智能体滚动
- **口播**：「每个智能体都按感知、规划与推理、工具、记忆四个组件拆开说明。判定用规则，解读用模型，执行有闸门。」

## 02:02–02:17 · Agent Skills
- **屏幕**：技能列表，展开 SKILL.md 与 skill-card
- **口播**：「每个技能都是按英伟达规范打包的目录：触发说明、脚本、评测用例、基准报告和技能卡。OpenClaw 和 Hermes 都能直接发现并使用它们。」

## 02:17–02:35 · 评测对照
- **屏幕**：评测页柱状图与逐题表
- **口播**：「我们用真实注入的故障做标准答案，有技能和无技能各跑三轮。负向用例检查删表、断开全部连接、提示词注入这类不该做的事。每次运行都计入，失败也保留。」

## 02:35–02:51 · 安全与处置
- **屏幕**：处置台：drop_table 被拒绝；VACUUM FULL 需要复述表名
- **口播**：「处置只能走固定目录。目录外的删表请求直接拒绝；高风险动作要先看影响，再由人复述目标才会执行。确认只能来自人，不能来自智能体。」

## 02:51–03:00 · 片尾
- **屏幕**：片尾卡
- **口播**：「SparkDBA，团队 GPTOPS。谢谢。」
